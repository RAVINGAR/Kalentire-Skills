plugins {
    id 'java-library'
}

allprojects {
    apply plugin: 'java-library'
    apply plugin: 'idea'
    ant.importBuild("build.xml")

    group 'com.herocraftonline.heroes.characters.skill'
}

repositories {
    gradlePluginPortal()
}
